#
# Makefile for the Marvell Watchdog Driver
#
ifeq ($(CONFIG_ARCH_FEROCEON_KW2),y)
	include $(CURDIR)/arch/arm/mach-feroceon-kw2/config/mvRules.mk
endif

obj-$(CONFIG_FEROCEON_WATCHDOG) += feroceon_wdt.o
